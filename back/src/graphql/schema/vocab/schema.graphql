type Vocab {
  id: String
  word: String
  translations: [String]
  examples: [String]
}

extend type Query {
  listVocabs: [Vocab!]
}

extend type Mutation {
  addVocab(word: String!, translations: [String], examples: [String]): Vocab
}
